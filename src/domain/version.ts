import type {InstalledApplication,Release} from "./model";
export function compareAndroidRelease(release:Release,installed:InstalledApplication):"update-available"|"installed"|"local-newer"|"identity-mismatch"{const a=release.artifacts.find(x=>x.platform==="android");if(!a||!a.packageIdentity||a.packageIdentity!==installed.packageIdentity)return"identity-mismatch";if(a.signingCertificateSha256&&installed.signingCertificateSha256&&a.signingCertificateSha256!==installed.signingCertificateSha256)return"identity-mismatch";const r=a.versionCode??release.version.code??0;if(r>installed.versionCode)return"update-available";if(r<installed.versionCode)return"local-newer";return"installed"}
