import {useState} from "react";
const pipeline=[["01","Build","Your existing project builds its own release."],["02","Release","The finished artifact is published by its source provider."],["03","Ingest","AEM receives and normalizes the release event."],["04","Distribute","AEM publishes the release to the appropriate channel."]];
export default function App(){const[dark,setDark]=useState(true);return <main className={dark?"shell dark":"shell"}><header><div className="brand"><span>A</span>EM</div><button onClick={()=>setDark(v=>!v)}>{dark?"Light":"Dark"}</button></header><section className="hero"><p className="eyebrow">PERSONAL SOFTWARE STORE</p><h1>Build elsewhere.<br/><em>Distribute here.</em></h1><p className="lead">AEM connects your projects, releases and devices into one software distribution system.</p></section><section className="pipeline">{pipeline.map(([n,t,d])=><article key={n}><small>{n}</small><h2>{t}</h2><p>{d}</p></article>)}</section><section className="status"><div><span className="dot"/> Release pipeline foundation ready</div><span>Android · Windows · Linux · macOS · Web</span></section></main>}
