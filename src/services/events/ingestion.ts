import type {ReleaseEvent} from "../../domain/model";import type {ReleaseSourceProvider} from "../providers/provider";import type {CatalogStore} from "../storage/contracts";
export class ReleaseIngestionService{constructor(private readonly store:CatalogStore,private readonly providers:Record<string,ReleaseSourceProvider>){}async ingest(event:ReleaseEvent){const provider=this.providers[event.provider];if(!provider)throw new Error("Unsupported release provider: "+event.provider);if(!(await provider.verifyEvent(event)))throw new Error("Release event verification failed");const release=await provider.normalizeReleaseEvent(event);await this.store.saveRelease(release);return release.id}}
